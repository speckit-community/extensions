on:
  repository_dispatch:
    types: [on-catalog-trigger]
jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      catalogCommitHash: ${{ github.event.client_payload.commitHash }}
    secrets: inherit
